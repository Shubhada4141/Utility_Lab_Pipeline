name: UtilityHub CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Check Docker Version
        run: docker --version

      - name: Build Docker Image
        run: docker build -t utilityhub .